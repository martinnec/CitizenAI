# Requirements for CitizenAI Government Services Store
# This project uses only Python standard library modules for now

# Future dependencies (when implementing external store integration):
# requests>=2.28.0
# aiohttp>=3.8.0
# pydantic>=1.10.0
# sqlalchemy>=1.4.0 (if using database backend)
# redis>=4.3.0 (if using Redis for caching)

# Government Services Store Dependencies
rdflib>=6.0.0
openai>=1.0.0
chromadb>=0.4.0
numpy>=1.24.0
